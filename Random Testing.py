import time
import keyboard
from tqdm import tqdm
from tkinter import *
import tkinter as tk

"""

For more info and other tk stuff:
https://www.geeksforgeeks.org/python/python-gui-tkinter/+

"""
"""root = tk.Tk()

tk.Label(root, text = "Label Test").grid(row = 0, column = 0)       # Label - Text for the tk window
                                                                            # ___.pack() to make it show up in the tk window

tk.Button(root, text = "Test", width = 25).grid(row = 1, column = 0) # Button


tk.Button(root, text="Exit", width = 25, command = root.destroy).grid(row = 0, column = 1)   #command = root.destroy is to close the application/window

entry = tk.Entry(root)
entry.grid(row = 1, column = 1)

var = tk.IntVar()
tk.Checkbutton(root, text="Test", variable=var).grid(row=2, sticky=tk.W)




root.mainloop()"""


"""keyboard.press("F11")"""  #presses a key




"""
while True:
    try:
        if keyboard.is_pressed('q'):
            print('You Pressed q')
            break
    except:
        break 
"""

"""#print("Test Backspace")
          print("normal mode selected press Backspace again to confirm")
          while True:
              timer -= 1
              time.sleep(0.7)
              if keyboard.is_pressed("Backspace"):"""